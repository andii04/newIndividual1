public class A350 extends Airplane {

    public A350(int buildYear, boolean isClosed, boolean isLocked, boolean isOpen, boolean isUnlocked, int maxSpeed, String name) {
        super(buildYear, isClosed, isLocked, isOpen, isUnlocked, maxSpeed, name);
    }
}
