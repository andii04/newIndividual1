public class A380 extends Airplane{

    public A380(int buildYear, boolean isClosed, boolean isLocked, boolean isOpen, boolean isUnlocked, int maxSpeed, String name) {
        super(buildYear, isClosed, isLocked, isOpen, isUnlocked, maxSpeed, name);
    }
}
